title: Octopus DevOps
description: > # this means to ignore newlines until "baseurl:"
permalink: ':title/'
baseurl: "/devops" # the subpath of your site, e.g. /blog
url: "https://octopus.com" # the base hostname & protocol for your site, e.g. http://example.com

# Build settings
markdown: kramdown

include: [_pages]
exclude: ["node_modules", "package.json", "package-lock.json", "yarn.lock", "LICENSE", "README.md"]

collections:
  continuous-delivery:
    output: true
    permalink: /continuous-delivery/:title/
